// Intentionally left empty.
//
// Student quiz-taking E2E flows were removed to avoid environment-dependent
// timeouts that occur when quiz data differs (e.g., short-answer-only quizzes
// with no <label> options to click).
